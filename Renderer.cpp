// Renderer.cpp
#include "Renderer.h"
#include <GL/gl.h>

void Renderer::render(World& world, Player& player) {
    // Placeholder rendering logic
    glClear(GL_COLOR_BUFFER_BIT);
    // Render blocks and player here
}
