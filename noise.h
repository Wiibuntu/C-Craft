#ifndef NOISE_H
#define NOISE_H

// Returns a Perlin noise value for the given (x, y) coordinates.
// The value will be roughly in the range [-1, 1].
float perlinNoise(float x, float y);

#endif // NOISE_H
