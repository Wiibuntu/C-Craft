#ifndef GLOBALS_H
#define GLOBALS_H

#include <GL/glew.h>

// Declare them as extern so multiple .cpp files can share them
extern GLuint worldShader;
extern GLuint texID;
extern int SCREEN_WIDTH;
extern int SCREEN_HEIGHT;

#endif

